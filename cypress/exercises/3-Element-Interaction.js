// Step 1: Visit https://www.demoblaze.com/index.html 

// Step 2: Select the 'Samsung Galaxy s6' link from the homepage

cy.get('').contains('').click(); // change this to the correct selector and text

// Step 3: Verify that the 'Samsung Galaxy s6' page is visible by verfiying the 'Samsung Galaxy s6' text is visible for the .name class header

cy.get('').should(); // change this to the correct selector and assertion (hint: the element should have the correct text)

// Step 4: Select the 'Add to cart' button

cy.get('').click()
